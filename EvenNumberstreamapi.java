package streamapi;
import java.util.*;
public class EvenNumber {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*ArrayList<Integer> list=new ArrayList<>();
		list.addAll(List.of(10,15,20,25,30,11,17,16));
		list.stream().filter(n->n%2==0).forEach((n)->System.out.print(n+" "));
		*/
		/*ArrayList<String> name=new ArrayList<>();
		List<String> list=List.of("Amit","Anil","Ravi","Vikas","Ashok","Rahul");
		name.addAll(list);
		name.stream().filter(n->n.startsWith("A")).forEach(System.out::println);
		*/
	/*	List<Integer> list=Arrays.asList(10,60,45,90,30,24,50,76);
		ArrayList<Integer> l1=new ArrayList<>();
		l1.addAll(list);
		l1.stream().filter(n->n>=50 &&n<=60).forEach(System.out::println);
		*/
		/*List<String> list=Arrays.asList("Java","Python","C","React","C#","Next.js","Node.js");
		ArrayList<String> l1=new ArrayList<>();
		l1.addAll(list);
		l1.stream().filter(n->n.length()>4).forEach(System.out::println);
		*/
		/*List<String> list=Arrays.asList("Java",null,"Spring",null,"React");
		ArrayList<String> l1=new ArrayList<>();
		List<String> notNullList=l1.stream().filter(n->n!=null).toList();
		System.out.println(notNullList);*/
		
	}

		
}
